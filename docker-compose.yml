services:
  rabbitmq:
    image: rabbitmq:4-management
    container_name: genflow-rabbitmq
    ports:
      - "5672:5672"
      - "15672:15672"
    environment:
      RABBITMQ_DEFAULT_USER: genflow
      RABBITMQ_DEFAULT_PASS: genflow123
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq

volumes:
  rabbitmq_data: